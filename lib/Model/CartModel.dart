class CartModel {
  final String image, name, price;
  final int id, itemCount;
  CartModel({
    required this.name,
    required this.price,
    required this.id,
    required this.image,
    required this.itemCount,
  });
}
